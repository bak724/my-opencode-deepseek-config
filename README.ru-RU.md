# My OpenCode × DeepSeek Config

[简体中文](README.md) | [繁體中文](README.zh-TW.md) | [English](README.en-US.md) | **Русский** | [Français](README.fr-FR.md) | [Deutsch](README.de-DE.md) | [Español](README.es-ES.md) | [Português](README.pt-BR.md) | [日本語](README.ja-JP.md) | [한국어](README.ko-KR.md)

**Оптимальная конфигурация OpenCode × DeepSeek** — конфигурация, раскрывающая возможности связки из двух моделей DeepSeek V4 (Pro + Flash) на максимум в мульти-агентном фреймворке OpenCode. Ключевой принцип: **эффективность токенов прежде всего — достижение наилучших результатов разработки при минимальных затратах контекста**.

## Обзор текущей конфигурации

- Основной агент по умолчанию: `orchestrator`
- Основная модель: `deepseek/deepseek-v4-pro`, лёгкая модель: `deepseek/deepseek-v4-flash`
- Глубина субагентов: `subagent_depth: 3` (поддерживает 3 уровня вложенности агентов)
- Изоляция моделей: двойная блокировка `enabled_providers: ["deepseek"]` + `disabled_providers`
- Общий доступ к сессии: отключён (`share: "disabled"`); снимки: включены (`snapshot: true`)
- Базовые разрешения: по умолчанию разрешено, деструктивные bash-команды — `ask`; чувствительные файлы (`.env`) — `deny`; внешние каталоги — `ask`
- Сжатие контекста: проактивное сжатие DCP (порог 35K–75K) + нативная compaction OpenCode как запасной вариант
- Глобальные правила: `AGENTS.md` (ключевые принципы, контракт отказа от задач, управление контекстом и эффективность токенов, самопроверка, анти-паттерны и т.д.)
- Навыки: **16** навыков `SKILL.md` в каталоге `skills/`, загружаемых по требованию через нативный инструмент `skill`
- Плагины: `superpowers` (14 процессных навыков), `@tarquinen/opencode-dcp` (интеллектуальная обрезка контекста)
- Экспериментальные функции: `batch_tool` включён по умолчанию

## Конфигурация моделей DeepSeek

### Предварительные требования

- OpenCode ≥ v1.14.24 (провайдер DeepSeek встроен)
- API-ключ DeepSeek: получите на [platform.deepseek.com/api_keys](https://platform.deepseek.com/api_keys)

### Способ 1: Интерактивная настройка через TUI (рекомендуется)

```bash
opencode
# В TUI введите: /connect → выберите DeepSeek → вставьте API-ключ
# Затем: /models → выберите deepseek-v4-pro
```

API-ключ будет автоматически сохранён в `~/.local/share/opencode/auth.json`.

### Способ 2: Переменные окружения

Windows PowerShell:
```powershell
$env:DEEPSEEK_API_KEY="sk-your-key-here"
opencode
```

Постоянная настройка: добавьте `DEEPSEEK_API_KEY` в системные переменные окружения.

### Справочник конфигурации провайдера

```jsonc
{
  "model": "deepseek/deepseek-v4-pro",
  "small_model": "deepseek/deepseek-v4-flash",
  "enabled_providers": ["deepseek"],
  "disabled_providers": ["openai", "anthropic", "google", "openrouter"]
}
```

Чтобы включить thinking/reasoning для модели Pro, добавьте в `provider`:

```jsonc
"provider": {
  "deepseek": {
    "models": {
      "deepseek-v4-pro": {
        "options": {
          "thinking": { "type": "enabled" }
        }
      }
    }
  }
}
```

> **Правило именования ID моделей**: `provider_id/model_id`, т.е. `deepseek/deepseek-v4-pro` и `deepseek/deepseek-v4-flash`.

## Установка и развёртывание

### Способ 1: Клонирование в глобальный каталог конфигурации (рекомендуется)

**Windows (PowerShell):**

```powershell
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.config"
git clone https://github.com/znlgis/my-opencode-deepseek-config.git "$env:USERPROFILE\.config\opencode"
```

**Linux / macOS:**

```bash
git clone https://github.com/znlgis/my-opencode-deepseek-config.git ~/.config/opencode
```

> **Примечание о совместимости**: `~/.config/opencode` — стандартный путь глобальной конфигурации OpenCode. Файлы `agents/`, `skills/`, `AGENTS.md` в этом репозитории следуют принятому в OpenCode макету и автоматически распознаются после клонирования без дополнительной настройки.

### Способ 2: Клонирование в произвольное место + переменная окружения

```powershell
$env:OPENCODE_CONFIG_DIR = "D:\path\to\opencode-config"
opencode
```

### Проверка установки

Запустите OpenCode и проверьте:
1. `/models` → текущая модель `deepseek/deepseek-v4-pro`
2. В списке агентов должны отображаться 10 агентов: `orchestrator`, `planner`, `deep-worker` и др.
3. Введите любой запрос — Orchestrator автоматически проанализирует намерение и направит его нужному агенту

## Распределение моделей

Этот репозиторий строго ограничен распределением задач между двумя моделями DeepSeek V4, без привлечения других моделей:

| Модель | Назначение |
| --- | --- |
| `deepseek/deepseek-v4-pro` | Планирование, архитектура, анализ первопричин, код-ревью, сложная реализация, основная диспетчеризация |
| `deepseek/deepseek-v4-flash` | Быстрое исследование, внешний поиск, лёгкие задачи, простые правки |

### Стратегия маршрутизации

- **Flash в первую очередь**: чётко определённые задачи — поиск, просмотр, простые правки — направляются flash-агентам
- **Pro для рассуждений**: планирование, анализ, ревью, сложная реализация — только pro
- **Автоматическое повышение**: если flash-агент не справляется, задача автоматически передаётся pro (с полным контекстом)

## Структура агентов

### Основной агент

| Агент | Модель | Роль |
| --- | --- | --- |
| `orchestrator` | v4-pro | Точка входа по умолчанию: шлюз намерений (Intent Gate) + маршрутизация с учётом модели + цепочка резервирования |

### Субагенты

| Агент | Модель | Права | Роль |
| --- | --- | --- | --- |
| `planner` | v4-pro | чтение/запись | Планирование, архитектура, декомпозиция задач |
| `deep-worker` | v4-pro | чтение/запись | Сложная реализация, многофайловые изменения, сложная отладка |
| `oracle` | v4-pro | **только чтение** | Анализ первопричин, глубокое понимание кода |
| `reviewer` | v4-pro | **только чтение** | Двухосевое код-ревью (стиль + спецификация) + калибровка серьёзности |
| `ui-builder` | v4-pro | чтение/запись | Задачи, связанные с фронтендом и UI |
| `consultant` | v4-pro | чтение/запись | Обсуждение решений, рекомендации по лучшим практикам |
| `explore` | v4-flash | **только чтение** | Поиск по кодовой базе, параллельное исследование |
| `librarian` | v4-flash | **только чтение** | Поиск по документации, веб-поиск |
| `light-orchestrator` | v4-flash | чтение/запись | Лёгкие задачи, правки одного файла |

> `deep-worker` и `light-orchestrator` следуют принципу «запрет исследований, запрет делегирования» — исполнение, а не исследование; контекст предоставляется orchestrator.

## Быстрые команды

### Команды маршрутизации агентов

| Команда | Агент | Назначение |
| --- | --- | --- |
| `/deep` | `deep-worker` | Сложная реализация, многофайловые изменения |
| `/quick` | `light-orchestrator` | Лёгкие задачи, правки одного файла |
| `/ui` | `ui-builder` | Работа с фронтендом/UI |
| `/review` | `reviewer` (code-review) | Двухосевое параллельное ревью (стиль + спецификация) + калибровка серьёзности |
| `/review-pr` | `reviewer` (code-review + gh-cli) | Ревью PR с публикацией комментариев на GitHub |
| `/plan` | `planner` | Составление планов, технических решений |
| `/search` | `librarian` | Внешний поиск, поиск по документации |
| `/oracle` | `oracle` | Глубокий анализ, выяснение причин проблем |
| `/consult` | `consultant` | Консультации, сравнение, рекомендации |

### Операционные команды

| Команда | Агент | Назначение |
| --- | --- | --- |
| `/commit` | `light-orchestrator` | Генерация сообщения коммита в формате Conventional Commits |
| `/release` | `deep-worker` (git-release) | Подготовка релиза с тегом |
| `/reflect` | `oracle` (reflect) | Выявление проблем → предложения по оптимизации конфигурации |
| `/handoff` | `light-orchestrator` (handoff) | Сжатие сессии в документ передачи |

### Встроенные команды

| Команда | Агент | Назначение |
| --- | --- | --- |
| `/codemap` | `explore` (codemap) | Генерация карты структуры репозитория |
| `/simplify` | `oracle` (simplify) → `light-orchestrator` | oracle анализирует → light-orchestrator применяет упрощение |
| `/rmslop` | `deep-worker` (remove-deadcode) | Очистка мёртвого кода и AI-шлака |

### Команды спецификации

| Команда | Агент | Назначение |
| --- | --- | --- |
| `/spec-propose` | `planner` (spec-workflow) | Исследование кода → черновик предложения по изменениям |
| `/spec-apply` | `deep-worker` (spec-workflow) | Пошаговая реализация по tasks.md → автоархивирование |

## Навыки (Skills)

OpenCode предоставляет навыки по требованию через нативный инструмент `skill` — агент загружает их только при необходимости, не расходуя контекст.

| Навык | Назначение |
| --- | --- |
| `code-review` | Двухосевое параллельное ревью (стиль + спецификация) + калибровка серьёзности |
| `codemap` | Генерация аннотированной карты репозитория, экономия токенов на исследование |
| `gh-cli` | Полный справочник GitHub CLI v2.96+ (Issues 2.0, copilot, agent-task, gh skill) |
| `git-master` | Продвинутые операции Git: rebase, squash, bisect, reflog, worktree |
| `git-release` | Релиз с тегом: определение SemVer, примечания к релизу, команда gh release |
| `handoff` | Сжатие сессии в документ передачи (ссылки на пути, без копирования содержимого) |
| `opencode-config` | Создание и поддержка конфигурации OpenCode |
| `reflect` | Непрерывное улучшение: выявление проблем → минимальные исправления |
| `remove-deadcode` | Безопасный поиск и удаление мёртвого кода, проверка LSP перед удалением |
| `security-review` | Аудит безопасности diff перед слиянием |
| `shared-language` | Построение глоссария предметной области, значительная экономия токенов контекста |
| `simplify` | Поведенчески-сохраняющее упрощение кода (oracle анализирует → light-orchestrator применяет) |
| `spec-workflow` | Лёгкий процесс изменений по спецификации (propose → design → tasks → implement → archive) |
| `verification-planning` | Планирование минимального пути верификации перед реализацией |
| `verify-with-docs` | Сверка с API-документацией перед кодированием, приоритет поиска, предотвращение галлюцинаций |
| `writing-great-skills` | Стандарт написания навыков: обрезка холостых операций, позитивные формулировки, критерии завершения |

## Проектные решения и история итераций

Ключевые идеи заимствованы у [oh-my-openagent](https://github.com/code-yeongyu/oh-my-openagent) (шлюз намерений, изоляция только-чтение, анти-паттерны), [oh-my-opencode-slim](https://github.com/alvinunreal/oh-my-opencode-slim) (приоритет диспетчера, цепочка резервирования, контракт отказа), [anomalyco/opencode](https://github.com/anomalyco/opencode) (схема конфигурации, система навыков), [cli/cli](https://github.com/cli/cli) (полный набор команд gh), [OpenSpec](https://github.com/Fission-AI/OpenSpec) (дельта-спецификации, обновление предложений по изменениям), [mattpocock/skills](https://github.com/mattpocock/skills) (документы передачи, структурированная отладка), [pi](https://github.com/earendil-works/pi) (сначала ответ, потом правка; лаконичные ответы) и [deepreview](https://github.com/mechanai/deepreview) (сканирование энтропии, проверка сходимости). Чисто конфигурационное решение, ноль дополнительных зависимостей.

> **Заимствование, а не копирование**: из тяжёлых пайплайнов извлекаются только принципы облегчённого дизайна; избыточные функции покрываются существующими agents/skills, новые не добавляются. Следуем принципу «упрощение важнее дополнения», каждая итерация нацелена на чистое сокращение токенов.

### Вехи итераций

| Этап | Ключевые изменения |
| --- | --- |
| **v1–v7 (Основа)** | Привязка двух моделей, система ролей агентов, шлюз намерений/классифицирующая маршрутизация, глобальные правила AGENTS.md, каталог навыков и псевдонимы команд, базовые разрешения |
| **v8–v12 (Ревью + Спецификации)** | Улучшение code-review (уровни/самопроверка/критерии отказа), создание spec-workflow (explore→propose→apply→archive), добавление deepwork/reflect/verification-planning, gh-cli синхронизация с v2.96+ |
| **v13–v15 (Контракты + Оптимизация)** | Добавление Evidence Discipline / Task Rejection Contract / stop condition в AGENTS.md; полная дедупликация промптов агентов и глобальных правил; проверка ошибок фоновых субагентов |
| **v16–v18 (Эффективное исполнение)** | Удаление мифических названий, объединение таблиц маршрутизации, расширение gh-cli до Issues 2.0, добавление verify + фреймворка принятия решений в spec-workflow |
| **v19 (Синхронизация с upstream)** | Сверка с 6 upstream-репозиториями; исправление бага построчных комментариев `/review-pr`; маршрутизация code-review переведена с количества строк на объём логики |
| **v20 (Рефакторинг и оптимизация)** | `agent/`→`agents/` для соответствия рекомендациям OpenCode; AGENTS.md сокращён на 22% (290→227 строк); добавлены навыки `diagnose` (6-этапная отладка) + `handoff` (передача сессии); spec-workflow добавлена `/update`; code-review добавлено сканирование энтропии + проверка сходимости; дедупликация промптов агентов на 20% |
| **v21 (Полная оптимизация)** | Навыки 18→17 (удалены deepwork/conventional-commits/diagnose, добавлены writing-great-skills/shared-language); команды 29→18 (−38%); AGENTS.md 227→212 строк (−7%); построчная no-op обрезка навыков. Двухосевое параллельное code-review + механизм калибровочных файлов. Практический опыт из 6 репозиториев (pi/deepreview/mattpocock и др.). |
| **v22 (Валидация схем и оптимизация)** | Сверка с официальными схемами OpenCode и DCP: удалён нерабочий ключ `agent.fallback`; подтверждена валидность всех ключей `dcp.jsonc` (v3.1.14), ноль изменений вслепую; AGENTS.md: «Эффективность токенов» объединена с «Управление контекстом», полная дедупликация, исправлена висячая ссылка `Self-Verification` (212→197 строк); orchestrator: три таблицы маршрутизации объединены (128→79 строк, −38%, Intent Gate/Agent Directory/Fallback сохранены); 14 навыков: удалён frontmatter `license/compatibility/metadata`, игнорируемый парсером (−70 строк); `tool_output` понижен для активной экономии токенов (1500 строк/40 КБ). |
| **v23 (Интеграция дисциплин + оптимизация слияния)** | Финальная интеграция на основе 6 upstream-репозиториев: удалён `gh-skill` (функционал объединён с разделом Agent Skills в `gh-cli`, −122 строки); исправлена мёртвая ссылка `verification-planning`; AGENTS.md: добавлены две дисциплины из pi (сначала ответ — потом правка + явная позиция, глобальная лаконичность) + контракт делегирования slim + job board + файловый IPC deepreview (+15 строк); таблица orchestrator: реорганизована в подтаблицы Pro/Flash, 79→86; reviewer: добавлена позиция отказа по умолчанию для проверяющего (+6 строк); раздел Agent Skills в gh-cli усилен (+10 строк). Чистое сокращение ~90 строк, навыки 17→16. |

## Структура репозитория

```text
├── .ai/
│   └── calibration.yml           # Калибровка серьёзности code-review
├── agents/                       # 10 специализированных агентов
│   ├── orchestrator.md           # Точка входа: шлюз намерений + маршрутизация с учётом модели
│   ├── planner.md                # pro: архитектура и планирование
│   ├── deep-worker.md            # pro: сложная реализация
│   ├── oracle.md                 # pro: глубокий анализ кода (только чтение)
│   ├── reviewer.md               # pro: двухосевое код-ревью (только чтение)
│   ├── consultant.md             # pro: обсуждение решений и рекомендации
│   ├── ui-builder.md             # pro: фронтенд и UI
│   ├── explore.md                # flash: поиск по кодовой базе (только чтение)
│   ├── librarian.md              # flash: внешний поиск (только чтение)
│   └── light-orchestrator.md     # flash: простые правки
├── skills/                       # 16 навыков, загружаемых по требованию
│   ├── code-review/              # Двухосевое параллельное ревью + калибровка серьёзности
│   ├── codemap/                  # Генерация карты структуры репозитория
│   ├── gh-cli/                   # Справочник GitHub CLI v2.96+
│   ├── git-master/               # Продвинутые операции Git
│   ├── git-release/              # Релиз с тегом
│   ├── handoff/                  # Сжатие сессии в документ передачи
│   ├── opencode-config/          # Мета-навык: создание конфигурации этого репозитория
│   ├── reflect/                  # Непрерывное улучшение
│   ├── remove-deadcode/          # Обнаружение и удаление мёртвого кода
│   ├── security-review/          # Чек-лист аудита безопасности
│   ├── shared-language/          # Глоссарий предметной области (экономия токенов)
│   ├── simplify/                 # Поведенчески-сохраняющее упрощение кода
│   ├── spec-workflow/            # Разработка по спецификации
│   ├── verification-planning/    # Планирование пути верификации перед реализацией
│   ├── verify-with-docs/         # Приоритетная сверка с API-документацией
│   └── writing-great-skills/     # Стандарт написания навыков
├── opencode.jsonc                # Основная конфигурация (18 команд)
├── AGENTS.md                     # Глобальные правила (~212 строк)
├── dcp.jsonc                     # Сжатие контекста DCP (DeepSeek 128K)
├── LICENSE
└── README.md
```

## Руководство по использованию

### Режим 1: Автоматическая маршрутизация Orchestrator (по умолчанию)

Опишите задачу естественным языком — Orchestrator автоматически проанализирует намерение, выберет наиболее подходящего агента и модель для выполнения.

```text
«Помоги разобраться с ошибкой в этом интерфейсе входа»     → oracle анализирует причину → диагностический отчёт
«Оптимизируй этот цикл, производительность ужасная»        → oracle анализирует → deep-worker выполняет оптимизацию
«Проверь этот PR»                                          → reviewer проводит многомерное ревью → отчёт с уровнями
«Хочу добавить функцию экспорта в модуль пользователей»    → planner составляет план → deep-worker реализует
«Как использовать API use() в React 19»                    → librarian ищет документацию → сигнатуры и примеры
```

### Режим 2: Прямой вызов через псевдонимы команд

| Сценарий | Команда |
| --- | --- |
| Сложная реализация / многофайловые изменения | `/deep` |
| Лёгкие правки / редактирование одного файла | `/quick` |
| Составление технического решения / архитектуры | `/plan` |
| Поиск багов / глубокий анализ | `/oracle` |
| Код-ревью | `/review` |
| Внешний поиск / поиск API | `/search` |
| Фронтенд / UI-задачи | `/ui` |
| Обсуждение решений / сравнение альтернатив | `/consult` |
| Структурированная отладка | `/oracle` |

### Типичные рабочие процессы

**Разработка новой функции (по спецификации):**
```text
/spec-propose  → /spec-apply  → /review
```

**Поиск багов:**
```text
/oracle  → /deep  → /rmslop  → /commit
```

**Код-ревью:**
```text
/review-pr   ← ревью PR + автоматическая публикация комментариев
/review      ← двухосевое параллельное ревью
```

## Философия дизайна

- **Чисто конфигурационное решение, ноль дополнительных зависимостей** — все возможности реализованы через `opencode.jsonc` + `agents/*.md` + `skills/*/SKILL.md` + `AGENTS.md`
- **Максимальное использование двух моделей DeepSeek V4** — Pro для рассуждений и принятия решений, Flash для поиска и лёгкого исполнения
- **Эффективность токенов прежде всего** — ссылки на пути вместо вставки файлов, загрузка навыков по требованию, многоуровневое управление сжатием
- **Плагины усиливают, но не перетягивают одеяло** — superpowers обеспечивает процессную дисциплину, DCP с интеллектуальным сжатием заменяет простое усечение
- **Разделение исполнения и исследования** — deep-worker/light-orchestrator: запрет исследований/делегирования; explore/librarian: запрет изменений
- **Непрерывное улучшение** — reflect систематизирует выявление проблем, code-review с двухосевой калибровкой гарантирует качество
